{
  "name": "absurdle-ingest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate-models": "node scripts/generate-sequelize-models.js",
    "migrate:undo": "npx sequelize-cli db:migrate:undo",
    "migrate:up": "npx sequelize-cli db:migrate",
    "sync-schema": "node scripts/sync-schema.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "pg": "^8.7.3",
    "pg-hstore": "^2.3.4",
    "rss-parser": "^3.12.0",
    "sequelize": "^6.21.2"
  },
  "devDependencies": {
    "sequelize-auto": "^0.8.8",
    "sequelize-cli": "^6.4.1"
  }
}
